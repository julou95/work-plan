import React from 'react';
import PropTypes from 'prop-types';
import Rellax from 'react-rellax';

const ToDo = ({}) => (
  <div>
		<h1>2nd section</h1>
		<Rellax as="div" speed={-1} className="adsf">this one goes slow</Rellax>
	</div>
);

ToDo.propTypes = {};
ToDo.defaultProps = {};
ToDo.displayName = '';

export default ToDo